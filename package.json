{
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "discord.js": "^14.12.1",
    "node-schedule": "^2.1.0"
  },
  "scripts": {
    "dev": "node --env-file=.env --test --watch index.js",
    "start": "node --env-file=.env index.js",
    "test": "cd tests; for f in *.js; do echo \"→ Running $f\"; node \"$f\"; cd - || exit 1; done"
  },
  "type": "module"
}
